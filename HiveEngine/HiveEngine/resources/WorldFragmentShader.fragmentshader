#version 330 core

// Interpolated values from the vertex shaders
in vec3 Position_worldspace;

// Ouput data
out vec3 color;

void main(){

	// Light emission properties
	// You probably want to put them as uniforms
	color = mix(vec3(0,0,.4), vec3(0.5), Position_worldspace.y);
}
